[phases.setup]
nixPkgs = ["python311", "libxml2", "libxslt", "pkg-config", "gcc"]

[phases.install]
cmds = ["pip3 install --upgrade pip", "pip3 install -r requirements.txt"]

[start]
cmd = "gunicorn backend:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1 --log-level info"
